<!--search hit-->
<td>
  <%= link_to h(record.subject), job_path(:id => record.id, :project_id => record.project_id) %>
</td>
<td colspan="2">
  <%= h(record.description) %>
</td>
<!--search hit end-->
