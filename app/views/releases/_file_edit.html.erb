<div id="<%= params[:div_id] %>">
  <% form_remote_tag :update => "uploadfiles_view", :url => updatefile_project_release_path( { 
        :updatefile_id => @file.id  }) do |f| -%>
<table class=FileItem >
<tr>
<th> <%=  Fileentity.columns_hash['path'].human_name -%> : </th>
<td> <%= @file.path %> </td>
</tr>
<tr>
<th> <%=  Fileentity.columns_hash['description'].human_name -%> : </th>
<td> <%= text_field_tag 'description', @file.description ,{:size => 20 } %>  </td>
</tr>
<tr>
<th> <%=  Fileentity.columns_hash['size'].human_name -%> : </th>
<td> <%= number_to_human_size(@file.size) %> </td>
</tr>

</table> <!-- "FileItem"-->
    <%= submit_tag _("Update") %> <%= submit_to_remote 'cancel_button', _('Cancel'), :url => viewfile_project_release_path({
            :project_id => @project.id,
            :id => @release.id,
            :div_id =>  params[:div_id],
            :editfile_id => @file.id
          }), :update => params[:div_id] %>
  <% end %>
</div>
