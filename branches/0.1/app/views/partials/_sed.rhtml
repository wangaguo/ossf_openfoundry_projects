<% if project.accepts_role?("Admin", current_user()) -%>
    <td><%= link_to 'Show', :action => 'show', :id => project.id %></td>
    <td><%= link_to 'Edit', :action => 'edit', :id => project.id %></td>
    <td><%= link_to 'Destroy', { :action => 'destroy', :id => project.id }, :confirm => 'Are you sure?', :method => :post %></td>
<% else %>
  <td><%= link_to 'Show', :action => 'show', :id => project.id %></td>
  <td>Edit</td>
  <td>Destroy</td>
<% end %>
