<% for column in Project.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @project.send(column.name) %><%= "(#{Project.status_to_s(@project.status)})" if column.name == 'status'%>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @project %> |
<%= link_to 'Back', :action => 'list' %>
